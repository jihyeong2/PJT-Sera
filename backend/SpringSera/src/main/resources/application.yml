server:
  port: 9999
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/sera;MODE=MYSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show-sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug